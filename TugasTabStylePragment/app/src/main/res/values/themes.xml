<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme without ActionBar -->
    <style name="Base.Theme.MyCourse" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Menonaktifkan Action Bar bawaan -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Warna dan font custom -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="android:fontFamily">@font/poppins_regular</item>
        <item name="android:textColor">#000000</item>
    </style>

    <!-- Theme with custom Toolbar -->
    <style name="Theme.MyCourse" parent="Base.Theme.MyCourse">
        <!-- Customize your theme with ActionBar if needed -->
    </style>
</resources>
